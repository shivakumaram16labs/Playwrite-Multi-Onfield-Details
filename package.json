{
  "name": "onfield-snapshot-gallery",
  "version": "1.0.0",
  "private": true,
  "description": "Excel-driven KBOCWWB on-field snapshot gallery with render-once disk cache",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "install-browser": "npx playwright install chromium"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "multer": "^1.4.5-lts.2",
    "playwright": "^1.63.0",
    "xlsx": "^0.18.5"
  }
}
